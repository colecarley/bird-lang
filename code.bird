

fn add(x: int, y: int) -> int {
    return x + y;
}

var result = add(1, 2);
print result;

fn double(x: int) -> int {
    return x * 2;
}

fn quadruple(x: int) -> int {
    return double(double(x));
}

print quadruple(4);

