fn add(x: int, y: int) -> int {
    var a = x + 3;
    var b = y;
    return true ? a + b : -1;
}

fn double(x: int) -> int {
    return x * 2;
}

fn quadruple(x: int) -> int {
    return double(double(x));
}

print 1 + 1;


var x = 20;
x += 2;
print x;

x -= 2;
print x;

x *= 2;
print x;

x/= 2;
print x;

x %= 2;
print x;

x = 1;
print x;