print 1 < 2 ? 1 : 0;

print 1 < 1.2;

print 3.4 * 1.2;

var x: int = 4;

x += 2;
print x;
x -= 2;
print x;

x *= 2;
print x;

x /= 2;
print x;

x %= 2;
print x;

fn add(x: int, y: int)-> int {
    return y - x;
}

print add(1,2);

fn ret_str(s: str) -> str {
    return s;
}

print ret_str("hello, world!");

fn addF(x: float, y: float) -> int {
    return x + y;
}

print addF(1.0, 2.0);